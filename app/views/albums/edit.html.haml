%h1
  Albums controller, edit action
= error_messages_for @album

= form_for(@album, :action => url(:album, @album)) do
  = partial :form
  %p
    = submit "Update"

= link_to 'Show', url(:album, @album)
|
= link_to 'Back', url(:albums)