%h1
  Songs controller, edit action
= error_messages_for @song

= form_for(@song, :action => url(:song, @song)) do
  = partial 'form'
  %p
    = submit "Update"

= link_to 'Show', url(:song, @song)
|
= link_to 'Back', url(:songs)